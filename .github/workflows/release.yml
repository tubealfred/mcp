name: Release MCPB

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    name: Build and attach Desktop Extension
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.12.1

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build MCPB
        run: pnpm build:mcpb

      - name: Publish GitHub release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            mcpb/dist/*.mcpb
            mcpb/dist/updates.json
          generate_release_notes: true
